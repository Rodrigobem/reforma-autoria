name: Sync to Supabase

on:
  push:
    paths:
      - 'data-sync.json'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sync data to Supabase
        run: python3 sync.py
